<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MatthewGormleyWordleProject.MainPage"
             NavigationPage.HasBackButton="False"
             >

    <ScrollView>
        <VerticalStackLayout>
            <Image Source="notwordle.png" HeightRequest="50" WidthRequest="50"/>
            <Label Text="Wordle" FontSize="20" WidthRequest="100" HeightRequest="50" HorizontalOptions="Center" VerticalOptions="Center"/>
            <Label Text="Get 6 chances to guess a 5-Letter Word" HorizontalOptions="Center" VerticalOptions="Center"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Button Grid.Column="0" x:Name="PlayButton" Text="Play" Clicked="openGamePage" HeightRequest="50" WidthRequest="75" HorizontalOptions="End"/>
                <Button Grid.Column="1" x:Name="ResultsButton" Text="Results" Clicked="openResultsPage" HeightRequest="50" WidthRequest="75" HorizontalOptions="Start"/>
            </Grid>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
